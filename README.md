# 2025-logic-system-kmap
This small repository implements a K-map (Karnaugh map) solver — a powerful tool for finding the minimum Sum of Products (SOP) form of Boolean expressions using C++.

The tool allows 2~4 variables of inputs 
